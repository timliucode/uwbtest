<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        UWB_RANGING：執行期權限（Android 13 / API 33+），使用前必須動態申請。
        此 App 的 minSdk = 33，所以所有裝置都需要此權限。
    -->
    <uses-permission android:name="android.permission.UWB_RANGING" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        android:required="false" → 讓 App 可安裝到沒有 UWB 硬體的裝置。
        CapabilityCheckScreen 會在執行期自行判斷硬體是否存在。
    -->
    <uses-feature
        android:name="android.hardware.uwb"
        android:required="false" />

    <application
        android:name=".UwbApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Uwbtest">

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Uwbtest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.UwbRangingService"
            android:foregroundServiceType="connectedDevice"
            android:exported="false" />

    </application>

</manifest>
